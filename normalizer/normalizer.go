package normalizer

import (
    "github.com/edebernis/sizematch-protobuf/build/go/items"
)

// Normalize items
func Normalize(item *items.Item) (*items.NormalizedItem, error) {
    return &items.NormalizedItem{}, nil
}
